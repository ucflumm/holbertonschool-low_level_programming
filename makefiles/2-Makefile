CC=gcc
OPT=-O0
CFLAGS=-Wall -Wextra 

SRC=main.c school.c
OBJ=$(patsubst %.c,%.o,$(SRC))

NAME=school
BINARY=$(NAME)

all: $(BINARY)

$(BINARY): $(OBJ)
	$(CC) -o $@ $^

# regular expression where % is a wildcard
%.o:%.c
	$(CC) $(CFLAGS) -c -o $@ $^

clean:
	rm -rf $(BINARY) $(OBJECTS)
