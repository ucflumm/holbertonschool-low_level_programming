#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - randomise an int and prints if +,- or zero
 * Return: 0 if successful, 1 for error
 */
int main(void)
{
	int i = 0;

	while (i != 10)
	{
		printf("%d", i);
		i++;
	}
	putchar('\n');
	return (0);
}
