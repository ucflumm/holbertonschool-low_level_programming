#include "main.h"

/**
 * print_diagsums - prints diagonal sums 
 * @a: pointer to array
 * @s: int size
 * 
*/

void print_diagsums(int *a, int size)
{
	while (n < size)
	{
		count1 += cpy[i];
		cpy++;
		i++;
		n++;
	}
	printf("%d, ", count1);
	cpy = a;
	while (n > 0)
	{
		count2 += a[i];
		a++;
		i--;
		n--;
	}
	printf("%d\n", count2);

}